#include <bits/stdc++.h>
using namespace std;

//2. Longest subarray | substring where [condition]
// Ques -> Find the length of Longest subarray where sum <= k
int main() {

    
    vector<int> a = {-1, 2, 3, 3, 4, 5, -1};
    int n = a.size();
    int k = 4; 

    
    
    return 0;
}